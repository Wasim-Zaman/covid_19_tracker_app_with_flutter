import 'package:flutter/material.dart';

class CountriesListWidget extends StatelessWidget {
  const CountriesListWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
